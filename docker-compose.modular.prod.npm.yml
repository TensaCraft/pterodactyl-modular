services:
  panel:
    networks:
      default: {}
      proxy:
        aliases:
          - panel
          - pterodactyl-panel

networks:
  proxy:
    external: true
    name: ${PROXY_NETWORK:-proxy}
