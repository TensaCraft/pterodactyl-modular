services:
  init:
    networks:
      default: {}
      proxy: {}

  panel:
    networks:
      default: {}
      proxy:
        aliases:
          - panel
          - pterodactyl-panel

  queue:
    networks:
      default: {}
      proxy: {}

  scheduler:
    networks:
      default: {}
      proxy: {}

networks:
  proxy:
    external: true
    name: ${PROXY_NETWORK:-proxy}
